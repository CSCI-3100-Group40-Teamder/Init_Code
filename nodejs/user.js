function userRegistration(varchar name, varchar email, varchar password, varchar phone_number, int age, varchar introduction, varchar sex, int prefernece_1, int prefernece_2, int prefernece_3){
    return bool success;
};

function userLogin( varchar user_id, varchar password){
    return bool success;
};

function accountDelete(varchar user_id, varchar password){
    return bool success;
}

function changeUserInforamtion(varchar name, varchar email, varchar password, varchar phone_number, int age, varchar introduction, varchar sex, int prefernece_1, int prefernece_2, int prefernece_3){
    return bool success;
};