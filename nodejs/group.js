function createGroup(varchar administrator_ID, int max_members, varchar group_name, varchar group_introduction){
    return varchar group_ID; //if success; else, -1 
}
    
function changeGroup( varchar administator_ID, int max_members, varchar group_name, varchar group_introduction){ 
    return bool success;
}

function deleteGroup( varchar group_ID, varchar administator_ID, varchar administator_password ){
    return bool success;
}
